# ==============================================================================
# EcoGallery Docker Environment Configuration
# ==============================================================================
# Copy this file to .env and update the values below
# cp .env.example .env
#
# IMPORTANT: Change default passwords and API keys before deployment!
# ==============================================================================

# ------------------------------------------------------------------------------
# Docker Image Configuration (for pre-built image deployment)
# ------------------------------------------------------------------------------
# If using docker-compose.prod.yml with pre-built images from a registry:
# DOCKER_IMAGE_PREFIX=yourusername
# IMAGE_TAG=latest

# ------------------------------------------------------------------------------
# API Security
# ------------------------------------------------------------------------------
# REQUIRED: This key is used to authenticate requests between frontend and backend
# Generate a secure random string (min 32 chars recommended) https://www.guidgenerator.com/
# Use double quotes if spaces or special characters are present
API_KEY=CHANGE_ME_TO_A_SECURE_RANDOM_STRING

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
POSTGRES_USER=ecogallery
# REQUIRED: Set a strong database password
# Use double quotes if spaces or special characters are present
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_DB_PASSWORD
POSTGRES_DB=ecogallery

# REQUIRED: Admin user password for initial setup
# Use double quotes if spaces or special characters are present
ADMIN_PASSWORD=CHANGE_ME_TO_SECURE_ADMIN_PASSWORD

# PostgreSQL data storage location (optional)
# If set, database will be stored at this path for easy backup/access
# Windows example: C:/Users/YourName/ecogallery-data/postgres
# Linux/Mac example: /home/yourname/ecogallery-data/postgres
# POSTGRES_DATA_PATH=

# ------------------------------------------------------------------------------
# Pictures Storage
# ------------------------------------------------------------------------------
# REQUIRED: Absolute path to your pictures directory
# Windows example: C:/Users/YourName/Pictures
# Linux/Mac example: /home/yourname/pictures
# Use double quotes if spaces or special characters are present
PICTURES_PATH=/path/to/your/pictures

# ------------------------------------------------------------------------------
# Virtual Albums Configuration (optional)
# ------------------------------------------------------------------------------
# Path to your virtual albums YAML file
# Create this file to define dynamic albums based on queries
# See documentation for YAML format
# Windows example: C:/Users/YourName/ecogallery/virtual albums.yml
# Use double quotes if spaces or special characters are present
VALBUM_YAML="./virtual_albums.yml"

# ------------------------------------------------------------------------------
# ONNX Models for Face Detection (optional)
# ------------------------------------------------------------------------------
# Path to the directory containing ONNX model files (ultraface, arcface)
# Required only when running the face detection service (--profile face)
# Download models from https://github.com/onnx/models
# System is using file from source code by default (./data/models)
# If you want to use custom models, set this variable to the path where you will mount them as a volume in docker-compose.yml
# ONNX_MODELS_PATH=/path/to/models

# ------------------------------------------------------------------------------
# Gallery File Filtering (optional)
# ------------------------------------------------------------------------------
# Customize which files and folders are included/excluded from the gallery.
# Values are comma-separated. If not set, these are the defaults that will be used.

# Files/folders matching these suffixes or prefixes will be skipped (not included in the gallery)
SKIP_SUFFIX=_skip,-skip,_pss,-pss,_noW,-noW
SKIP_PREFIX=skip_,skip-,pss_,pss-,noW_,noW-
SKIP_CONTAINS=EosRP,_thumbnails

# Files with these suffixes/prefixes are treated as feature (cover) photos for albums
FEATURE_PHOTO_SUFFIX=_label,_feature
FEATURE_PHOTO_PREFIX=label_,feature_

# Only these image/video extensions will be included (web-friendly formats)
IMAGE_EXTENSIONS=.jpg,.jpeg,.png,.webp
MOVIE_EXTENSIONS=.mp4,.mov,.avi,.3gp

# ------------------------------------------------------------------------------
# Email Configuration (for password reset emails)
# ------------------------------------------------------------------------------
# SMTP authentication type: Basic, OAuth2, or SendGrid
SMTP_AUTH_TYPE=Basic

# SMTP server settings
# Gmail example: smtp.gmail.com
# Outlook example: smtp.office365.com
# Yahoo example: smtp.mail.yahoo.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP credentials for Basic auth
# For Gmail: use your email and an App Password (not your regular password)
# Generate App Password: https://myaccount.google.com/apppasswords
# Requires 2-Step Verification enabled
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here

# Email "From" address (usually same as SMTP_USER)
SMTP_FROM=your-email@gmail.com

# OAuth2 settings (only for Gmail with SMTP_AUTH_TYPE=OAuth2)
# Leave blank if using Basic auth
# To get these: https://console.cloud.google.com -> Create OAuth Client -> Use OAuth Playground
# SMTP_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
# SMTP_OAUTH_CLIENT_SECRET=your-client-secret
# SMTP_OAUTH_REFRESH_TOKEN=your-refresh-token

# SendGrid settings (only for SMTP_AUTH_TYPE=SendGrid)
# Create an API key in SendGrid and set the verified From address
# SENDGRID_API_KEY=your-sendgrid-api-key
# SENDGRID_FROM=your-verified-from@domain.com

# ------------------------------------------------------------------------------
# Optional: Network Configuration
# ------------------------------------------------------------------------------
# Port for nginx reverse proxy (default: 80)
# if you already have a web server running then change this to avoid conflicts (for example 8080)
HTTP_PORT=80

# Base URL for API (leave it blank for Docker networking through nginx)
#NEXT_PUBLIC_API_BASE=http://localhost:5001     
#NEXT_PUBLIC_API_BASE=